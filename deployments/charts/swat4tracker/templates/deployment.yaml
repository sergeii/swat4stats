apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "swat4tracker.podname" (set . "Name" "uwsgi") }}
  labels:
    {{- include "swat4tracker.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.uwsgi.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 50%
      maxUnavailable: 25%
  selector:
    matchLabels:
      {{- include "swat4tracker.selectorLabels" (set $ "Backend" "uwsgi") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "swat4tracker.selectorLabels" (set $ "Backend" "uwsgi") | nindent 8 }}
      annotations:
      {{- if .Values.uwsgiExporter.enabled }}
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.uwsgiExporter.targetPort }}"
      {{- end }}
    spec:
      {{- with $.Values.image.pullSecrets }}
      imagePullSecrets:
          {{- toYaml . | nindent 8 }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.uwsgi.terminationGracePeriodSeconds }}
      initContainers:
        - name: {{ include "swat4tracker.podname" (set . "Name" "copystatic") }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["cp", "-r", "/app/static/.", "/var/www/static"]
          volumeMounts:
            - name: staticfiles
              mountPath: /var/www/static
      containers:
        - name: {{ include "swat4tracker.podname" (set . "Name" "sc-staticfiles") }}
          {{- with .Values.staticfiles.image }}
          image: "{{ .repository }}:{{ .tag | default "latest" }}"
          imagePullPolicy: {{ .pullPolicy }}
          {{- end }}
          ports:
            - name: nginx
              containerPort: {{ .Values.staticfiles.targetPort }}
              protocol: TCP
          volumeMounts:
            - name: staticfiles
              mountPath: /var/www/static
            - name: config
              mountPath: /etc/nginx/nginx.conf
              subPath: staticfiles.conf
          resources:
            {{- toYaml .Values.staticfiles.resources | nindent 12 }}
        {{- if .Values.uwsgiExporter.enabled }}
        - name: {{ include "swat4tracker.podname" (set . "Name" "uwsgi-exporter") }}
          {{- with .Values.uwsgiExporter.image }}
          image: "{{ .repository }}:{{ .tag | default "latest" }}{{ if .sha }}@sha256:{{ .sha }}{{ end }}"
          imagePullPolicy: {{ .pullPolicy }}
          {{- end }}
          args: ["--stats.uri", "http://localhost:1717"]
          ports:
            - name: exporter
              containerPort: {{ .Values.uwsgiExporter.targetPort }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /-/healthy
              port: exporter
        {{- end }}
        - name: {{ include "swat4tracker.podname" (set . "Name" "uwsgi") }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: uwsgi
              containerPort: {{ .Values.uwsgi.targetPort }}
              protocol: TCP
          env:
          {{- range $key, $val := .Values.uwsgi.env }}
            - name: {{ $key }}
              value: "{{ $val }}"
          {{- end }}
          {{- range $key, $val := .Values.sharedEnv }}
            - name: {{ $key }}
              value: "{{ $val }}"
          {{- end }}
          envFrom:
          {{- range .Values.envFromSecrets }}
            - secretRef:
                name: {{ tpl .name $ }}
                optional: {{ .optional | default false }}
          {{- end }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.uwsgi.resources | nindent 12 }}
      volumes:
        - name: staticfiles
          emptyDir: {}
        - name: config
          configMap:
            name: {{ include "swat4tracker.fullname" . }}
