apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "swat4tracker.fullname" . }}
  labels:
    {{- include "swat4tracker.labels" . | nindent 4 }}
data:
  {{- $files := .Files }}
  {{- range tuple "staticfiles.conf" }}
  {{ . }}: |-
        {{- $files.Get .  | nindent 6 }}
  {{- end }}
